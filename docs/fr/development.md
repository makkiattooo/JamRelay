# Développement

Lancez `npm ci`, `npm run format`, `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` et `npm run docs:build`. Pour un nouvel outil, mettez à jour l’enregistrement, les tests et la référence. Conservez les noms publics et n’ajoutez jamais de secrets ou de données Spotify réelles.
